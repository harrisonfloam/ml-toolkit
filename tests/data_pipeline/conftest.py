"""Pytest config.

This test suite assumes the package is importable (e.g., installed editable via the
repo root uv workspace).
"""

# Tests now run against the single-package layout under `ml_toolkit.*`.
