
# memory-layer

Experimental “memory layer” concept: store many text entries and roll them up into a hierarchy of summaries (implemented as a graph) so you can query at different levels of abstraction.

## Status

Work in progress. Current skeleton API may change.
